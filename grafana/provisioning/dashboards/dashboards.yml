apiVersion: 1

providers:
  - name: oncall-lab
    folder: OnCall Lab
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
